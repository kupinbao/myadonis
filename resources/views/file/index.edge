@layout('layouts.main')


@section('content')

<div class="container">
  <table class="table my-5">
    <thead>
      <tr>
        <th>名称</th>
        <th>类型</th>
        <th>大小</th>
      </tr>
    </thead>
    <tbody>
      @each(file in files)
      <tr>
        <td><a href="{{ route('files.show',{id:file.id})}}">{{file.client_name}}</a></td>
        <td>{{file.type}}</td>
        <td>{{file.size}}</td>
      </tr>
      @endeach
    </tbody>
  </table>

</div>

@endsection
